# Beakon runs on in-memory seed data by default — no env needed to start.
# To connect Supabase (Postgres + auth), copy this file to .env.local and fill in:
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
